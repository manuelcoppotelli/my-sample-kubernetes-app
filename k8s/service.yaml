apiVersion: v1
kind: Service
metadata:
  name: sample-microservice
  labels:
    app: sample-microservice
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: sample-microservice
